// Placeholder route for the gradient "+" tab button.
// The button intercepts presses and navigates to /snippets/new,
// so this screen should never actually render.
export default function Add() {
  return null;
}
